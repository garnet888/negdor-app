import { StyleSheet } from "react-native";

export default StyleSheet.create({
  form: {
    gap: 10,

    paddingHorizontal: 20,
    marginTop: 20,
  },

  signupBtn: {
    marginTop: 10,
  },
  loginBtn: {
    alignItems: "center",
  },
});
