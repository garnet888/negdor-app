import { StyleSheet } from "react-native";
import { colors } from "../../constants/constColors";

export default myLinkCss = StyleSheet.create({
  text: {
    color: colors.blue,
    fontSize: 18,
  },
});
