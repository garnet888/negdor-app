import { StyleSheet } from "react-native";

export default StyleSheet.create({
  input: {
    width: "100%",
    minHeight: 30,

    borderWidth: 1,
    borderColor: "gray",
    borderRadius: 6,

    paddingHorizontal: 6,
  },
});
